# Verified anchored cube duplication escape

Every nonempty subset S avoiding a zero anchor has a member whose
duplication leaves the entire anchored cube, at odd cyclic modulus.
The first theorem assumes injective doubling explicitly; the odd-modulus
corollary supplies it. The argument selects a largest return layer and
uses shortest-representation rigidity to force an impossible zero
coordinate if all duplications return.

Both results hold in arbitrary dimension and at every subset size.
They prove an escape for each subset, without asserting distinct escapes
for different subsets or linear independence of the proposed matrix.
The aggregate central outside-cube count and G1/G2/G3/CR1 remain open.

Both theorem nodes are private and Proved on Prove2Me. Verified
submissions and exact server proof-source readbacks pass. Both revisions
verify two target types and dependency sets, two external interface
types, two definition values and the original doubling-injectivity
helper type. No new definition bundle is introduced. One supporting
root retains the arbitrary-degree odd-modulus escape result.

1 submissions retain raw SKETCH_ACCEPTED verdicts; Proved statuses, exact sources and resolved dependencies are verified. No proof was resubmitted to change its classification.

The consolidated DAG has 1299 nodes and 2988 edges. All 845 exact proof dependency sets across 63 bundles match. G1/P6, G2/From7, G3/From7 and CR1 remain open.

The central outside-cube lower bound and CR1/G1/G2/G3 remain open.

* exists_duplicate_subset_sum_outside_cube: 2c986ef4-adf9-4d44-a7dc-a7191975e2ad
* exists_duplicate_subset_sum_outside_cube_of_odd: 55b7856d-1aca-413a-83b1-6aac806db14e
